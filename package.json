{
  "name": "trackfin",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["backend"],
  "scripts": {
    "start": "cd backend && node server.js",
    "install": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 